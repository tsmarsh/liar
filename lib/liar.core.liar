(ns liar.core
  (:require [liar.prelude :refer :all]))

;; liar.core is reserved for runtime-oriented definitions that may depend on
;; macro expansion. The macro-safe baseline lives in liar.prelude.
