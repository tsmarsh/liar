[package]
name = "lir-cert"
version = "0.1.0"
edition = "2021"
description = "Cucumber test harness for lIR feature specifications"

[dependencies]
cucumber = "0.21"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
async-trait = "0.1"
